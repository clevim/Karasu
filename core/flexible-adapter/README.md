# flexible-adapter (vendored)

Source of [arkon/FlexibleAdapter](https://github.com/arkon/FlexibleAdapter) at commit
`c8013533` — modules `flexible-adapter` and `flexible-adapter-ui` merged into one.
Changes: `SelectableAdapter` logs a fixed version string instead of `BuildConfig.VERSION_NAME`; the two `utils` classes import `androidx.appcompat.R` (the `-ui` module R no longer exists).

Apache-2.0, see [LICENSE](LICENSE).
