.gradle
/local.properties
/.idea/workspace.xml
.DS_Store
/build
.idea/
*iml
*.iml
*/build
*/*/build
.kotlin/
kls_database.db
weblate.conf

# Assinatura. O CI usa secrets.SIGNING_KEY, então nada disso deveria existir aqui —
# mas uma chave vazada não tem como ser desfeita.
*.jks
*.keystore
keystore.properties

# Android Studio profiler e heap dumps (o LeakCanary do debug gera .hprof sozinho)
captures/
*.hprof

# Claude Code / ferramentas locais — não versionar
.claude/
CLAUDE.md
graphify-out/
PLANO.md
